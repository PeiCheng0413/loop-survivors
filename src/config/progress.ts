/**
 * 成長曲線：經驗球、升級幅度、等級所需經驗。
 *
 * 升級卡的加成一律**加算而非乘算** —— 乘算疊十幾層會指數爆炸，
 * 實測曾把每秒傷害推到 1231（見 docs/DECISIONS.md §5b）。
 */

/**
 * 經驗球：敵人死亡後掉落，玩家要走過去撿。
 *
 * 為什麼不自動入袋：撿球讓「走位」從純粹的閃避變成資源獲取手段 ——
 * 玩家必須在「安全地待著」與「衝進敵群撿球」之間做取捨，
 * 而這正是走位有樂趣的來源。
 */
export const GEM = {
  radius: 5,
  /** 磁吸範圍。進入後會自己飛過來，避免玩家為了一顆球做像素級走位 */
  magnetRadius: 74,
  magnetSpeed: 460,
  value: 1,
};

/**
 * 升級卡的堆疊幅度。**一律用加算而非乘算。**
 *
 * 乘算會指數爆炸：傷害每級 ×1.2 疊 18 層是 26 倍，攻速 ×0.85 疊 18 層等於
 * 快 20 倍 —— 實測就是這樣把每秒傷害推到 1231。加算是線性成長，
 * 後期仍有感但不會失控。
 */
export const UPGRADE = {
  damage: 0.22,
  moveSpeed: 0.09,
  pickup: 0.3,
  /** 攻速用遞減式：冷卻 = 基準 ÷ (1 + haste × 堆疊數)，永遠不會歸零 */
  haste: 0.16,
  capacity: 2,
};

/**
 * 升級所需經驗：level 級升到下一級要幾顆球。
 *
 * 曲線要夠陡。每次升等都會暫停遊戲跳卡片，太頻繁的話一局會被打斷幾十次，
 * 節奏全毀 —— 而且學生根本來不及思考「這塊插哪」，升等反而變成干擾。
 * 目標是五分鐘一局升 15～20 級。
 */
export function xpForLevel(level: number): number {
  return 8 + level * 9;
}
